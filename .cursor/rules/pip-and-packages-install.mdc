---
description: when running a pip install command or wanting to add or change a project dependency in python
globs: 
alwaysApply: false
---
Note that we are using uv with venv so use that and NEVER use pip install directly.
Also, make sure to always update the [requirements.txt](mdc:backend/requirements.txt) file when adding/changing dependency rather than just running pip install.